# Palm Tree Dataset Scraper

This project scrapes online nurseries for palm tree images + metadata (species, age range, height) and builds a CSV dataset.

## Setup

1. Create a Python virtual environment:

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
